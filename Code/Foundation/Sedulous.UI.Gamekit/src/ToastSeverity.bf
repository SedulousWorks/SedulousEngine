namespace Sedulous.UI.Gamekit;

/// What a toast is telling the player, which picks its accent through the theme's semantic
/// status colours rather than a colour named at the call site.
enum ToastSeverity
{
	Info,
	Success,
	Warning,
	Error
}
